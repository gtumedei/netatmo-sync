{
  "name": "netatmo-sync",
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "dev": "cross-env NODE_ENV=development tsx scripts/dev.ts",
    "preview": "cross-env NODE_ENV=production node scripts/preview.js",
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --packages=external --outfile=dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "cross-env": "^7.0.3",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
